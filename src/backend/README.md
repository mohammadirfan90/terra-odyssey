# Terra Odyssey — Backend boundary

Own request validation, job state, scientific eligibility, analysis orchestration,
result provenance, and export. The backend returns qualified statuses and typed
caveats; the frontend must not infer them from missing fields.

